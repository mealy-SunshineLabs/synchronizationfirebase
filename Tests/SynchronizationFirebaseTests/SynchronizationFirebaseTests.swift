    import XCTest
    @testable import SynchronizationFirebase

    final class SynchronizationFirebaseTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SynchronizationFirebase().text, "Hello, World!")
        }
    }
